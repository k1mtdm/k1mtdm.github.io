---
import Navigation from "./Navigation.astro";
import { fade } from 'astro:transitions';
import '../styles/global.css';

---

<header class="absolute bg-black inset-x-0 top-0 z-50" transition:animate={fade({ duration: '0.4s' })}>
    <nav>
        <Navigation />
    </nav>
</header>

